# Ritesh Resume — React (Original CSS)

This React project preserves 100% of your original HTML resume design.

## How to run

```bash
npm install
npm run dev
```

It uses the same CSS and HTML layout. The modal and animations work identically.
